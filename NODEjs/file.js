const join = require("./cal.js")

// also can be done as const {add,sub} = require("./cal.js") then we dont need to make the join variable we can just simply use the function name.

console.log(join.sub(2,3));